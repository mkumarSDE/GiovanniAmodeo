---
 // No script needed
---

<style>
  .hero-bg {
    background-image: url('/bg-images.jpg');
    background-size: cover;
    background-position: center;
  }

  .blur-overlay {
    position: absolute;
    inset: 0;
    z-index: -1;
    transition: filter 0.3s ease;
  }

  .profile-pic {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    object-fit: cover;
    border: 6px solid #fff;
    margin-bottom: 30px;
  }

  .social-icon {
    font-size: 24px;
    color: white;
    transition: color 0.3s ease;
  }

  .social-icon:hover {
    color: #3b82f6; /* Tailwind blue */
  }

  .socials-container {
    position: absolute;
    bottom: 30px;
    right: 30px;
    background-color: rgba(0, 0, 0, 0.5);
    padding: 12px 16px;
    border-radius: 12px;
    display: flex;
    gap: 16px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.2);
  }
</style>

<section
  id="hero"
  class="relative min-h-screen flex flex-col justify-center items-center text-center px-4 overflow-hidden pt-24"
  x-data="{ blur: 20 }"
  x-init="
    const updateBlur = () => {
      blur = Math.max(0, 20 - window.scrollY / 10);
      $refs.bg.style.filter = `blur(${blur}px)`;
    };
    updateBlur();
    window.addEventListener('scroll', updateBlur);
  "
>
  <!-- Background blur -->
  <div
    x-ref="bg"
    class="hero-bg blur-overlay"
    style="filter: blur(20px);"
  ></div>

  <!-- Profile Picture -->
  <img
    src="/photo.jpg"
    alt="Profile"
    class="profile-pic"
  />

  <!-- Name and Description -->
  <h1 class="text-4xl md:text-6xl font-bold text-white drop-shadow mb-4 z-10">
    Giovanni Amodeo
  </h1>

  <p class="text-lg md:text-xl text-white max-w-xl mb-8 z-10 drop-shadow">
    Hi! My name is Giovanni Amodeo. People call me Gio.
  </p>

  <!-- Interviews Button -->
  <a
    href="#featured"
    class="inline-block px-6 py-3 bg-white text-blue-600 font-semibold rounded-full shadow-md hover:bg-gray-200 transition z-10"
  >
  See What I’ve Been Talking About
  </a>

  <!-- Social Icons
  <div class="socials-container z-10">
    <a href="https://www.linkedin.com/in/giovanniamodeo/" target="_blank" aria-label="LinkedIn">
      <i class="fab fa-linkedin social-icon"></i>
    </a>
    <a href="https://twitter.com/amodeog" target="_blank" aria-label="Twitter">
      <i class="fab fa-twitter social-icon"></i>
    </a>
    <a href="mailto:gio@example.com" aria-label="Email">
      <i class="fas fa-envelope social-icon"></i>
    </a>
  </div> -->
</section>
